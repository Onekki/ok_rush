
enum CategoryProperty {
  name, rushState
}

enum RushStateProperty {
  message, parser, endpoint, js, query
}

enum RushStateDataType {
  presets, input, parsed
}

enum PredicateType {
  repeat, restart, stop
}
